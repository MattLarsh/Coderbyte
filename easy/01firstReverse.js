function FirstReverse(str) {   
  return str.split("") /* put our string into an array of characters */
            .reverse() /* reverse our array of characters */
            .join(""); /* join our array back in a singe string */
         
}
